# Mobile Porter — TO-DO

- [x] **frogger (juego)** — sin paridad táctil → agregado D-pad de 4 direcciones (`TouchControls`), `RotateDeviceHint` y canvas responsivo en `app/games/frogger/play/page.tsx`, siguiendo el patrón de `serpentina` — _2026-06-22_
